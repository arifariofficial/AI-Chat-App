/* 
const results: SIPEEssay[] = await searchAPI(content);
  const context =
    results.length > 0
      ? results
          .map(
            (d) =>
              `Tiedot: ${d.content}\nLähde: ${d.source || "Tuntematon lähde"}.`,
          )
          .join("\n\n")
      : "Ei lisätietoja saatavilla.";

  return <div>ChatActionsUnused</div>;
};

File uploaded: {
  object: 'file',
  id: 'file-XREw7jY6YbPXAWPKzwzUkN',
  purpose: 'fine-tune',
  filename: 'fine_tune_data.jsonl',
  bytes: 26333,
  created_at: 1732321959,
  status: 'processed',
  status_details: null
}

 */
